[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "tollgate"
version = "0.1.0"
dependencies = [
    "click",
    "rich",
    "cryptography",
    "requests",
]

[project.scripts]
tollgate = "cli:cli"

[tool.setuptools.packages.find]
where = ["."]
